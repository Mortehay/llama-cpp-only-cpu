# Models fetched by the downloader container into $MODELS_DIR.
#
# IMPORTANT: this file is ONLY for the legacy llama.cpp `llm-server` service.
# The sprite generator does NOT read it — diffusers pulls its checkpoints via
# from_pretrained() on first use, into the same directory as an HF cache
# (models--stabilityai--sdxl-turbo/...). Adding a diffusers model here does
# nothing useful.
#
# Everything is commented out by default. These are multi-GB LLM downloads that
# `make up` blocks on, and none of them are needed to generate sprites.
# Uncomment only what you actually intend to run.

# --- Chat LLMs (legacy llm-server) ---------------------------------------
# bartowski/Llama-3.2-3B-Instruct-GGUF Llama-3.2-3B-Instruct-Q4_K_M.gguf
# bartowski/Mistral-7B-Instruct-v0.3-GGUF Mistral-7B-Instruct-v0.3-Q4_K_M.gguf
# bartowski/Llama-3.2-1B-Instruct-GGUF Llama-3.2-1B-Instruct-Q4_K_M.gguf
# bartowski/gemma-2-2b-it-GGUF gemma-2-2b-it-Q4_K_M.gguf

# --- Unverified repos ----------------------------------------------------
# These were in the file but their existence has not been confirmed; the
# downloader 404s on a missing entry and aborts the whole run (set -e).
# Verify on huggingface.co before uncommenting.
# bartowski/google_gemma-4-E4B-it-GGUF google_gemma-4-E4B-it-Q4_K_M.gguf
# llmfan46/gemma-4-E4B-it-uncensored-heretic-GGUF gemma-4-E4B-it-uncensored-heretic-Q4_K_M.gguf

# --- FLUX spritesheet experiment (dead) ----------------------------------
# tasks.py never loaded this GGUF: get_flux_pipeline expected
# /models/flux1-schnell.safetensors plus a hardcoded snapshot hash instead, so
# step 2 could not run at all. Step 2 now uses
# Onodofthenorth/SD_PixelArt_SpriteSheet_Generator via diffusers.
# unsloth/FLUX.2-klein-4B-GGUF flux-2-klein-4b-Q8_0.gguf
# fal/flux-2-klein-4b-spritesheet-lora flux-spritesheet-lora.safetensors
